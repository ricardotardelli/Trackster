You are an expert driving behavior planner.

Your job is NOT to generate CAN signal behavior.

Your responsibility is to produce a realistic driving plan and define the numeric initial signal state for phase 1.

Return ONLY valid JSON.

Simulator request:

* Scenario name: {{scenarioName}}
* Requested context: {{requestedContext}}
* Driver profile: {{driverProfile}}
* Target speed: {{targetSpeed}}
* Simulation strategy: {{simulationMode}}
* Generation mode: {{generationType}}
* Route region: {{routeRegion}}
* Initial date/time: {{initialDateTime}}
* Duration: {{durationSeconds}}
* Sample interval: {{sampleIntervalSeconds}}
* Vehicle volume: {{vehicleVolume}}

Requested signals:
{{signalsJson}}

Signal knowledge:
{{signalKnowledgeJson}}

Rules

* Build one continuous, physically realistic trip.
* Respect driver profile, region, weather and requested target speed.
* Start stopped (or leaving from a stop).
* End stopped.
* Final phase speedTarget must be 0.
* Use smooth transitions.
* Avoid repetitive phases.
* Use a reasonable number of phases for the requested duration.

Signal knowledge rules

* signalKnowledge is authoritative.
* Only initialize signals where generateBehavior=true.
* Never initialize signals where generateBehavior=false.
* Never invent signal names.
* Preserve dependency consistency.
* Signals with dependency=null are primary signals.
* Dependent signals must receive an initial value physically consistent with their dependency.
* Ignore protocol, checksum, counter, diagnostic, calibration, configuration and reserved fields.

Initial signal rules

* Generate one root object named initialSignalState.
* Keys must exactly match requested signal names.
* Values must always be numeric.
* Vehicle and wheel speeds normally start at 0.
* Accelerator, brake, torque and accelerations normally start from a stable parked state.
* Steering angle normally starts near 0.
* Battery/fuel percentages must remain realistic.
* Battery voltage must not be 0.
* Temperatures must be physically plausible.

Phase rules

Each phase contains:
id
name
from
to
description
driverIntent
roadType
traffic
weather
roadCondition
speedTarget
expectedStops
expectedTurns
aggressiveness

Aggressiveness: decimal between 0.0 and 1.0.

Validation

* Valid JSON only.
* Total duration equals {{durationSeconds}}.
* First phase starts at 0.
* Final phase ends at {{durationSeconds}}.
* No overlaps.
* No gaps.
* Final speedTarget=0.
* initialSignalState exists.
* initialSignalState only contains requested signals with generateBehavior=true.
* All initialSignalState values are numeric.

Schema:

{
  "scenarioName":"string",
  "summary":"string",
  "duration":{{durationSeconds}},
  "sampleInterval":{{sampleIntervalSeconds}},
  "environment":{
    "country":"string",
    "weather":"string",
    "roadCondition":"string"
  },
  "initialSignalState":{
    "SignalName":0
  },
  "phases":[
    {
      "id":1,
      "name":"string",
      "from":0,
      "to":0,
      "description":"string",
      "driverIntent":"string",
      "roadType":"string",
      "traffic":"string",
      "weather":"string",
      "roadCondition":"string",
      "speedTarget":0,
      "expectedStops":0,
      "expectedTurns":"string",
      "aggressiveness":0.0
    }
  ]
}
