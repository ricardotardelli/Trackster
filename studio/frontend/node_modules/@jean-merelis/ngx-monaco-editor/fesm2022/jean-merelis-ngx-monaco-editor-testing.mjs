import { ComponentHarness, HarnessPredicate, TestKey } from '@angular/cdk/testing';
import * as i0 from '@angular/core';
import { model, input, output, viewChild, signal, inject, NgZone, ChangeDetectorRef, ElementRef, forwardRef, Component, ChangeDetectionStrategy, ViewEncapsulation } from '@angular/core';
import * as i2 from '@angular/forms';
import { NG_VALUE_ACCESSOR, FormsModule } from '@angular/forms';
import * as i1 from '@angular/common';
import { DOCUMENT, CommonModule, NgClass, NgStyle } from '@angular/common';

class NgxMonacoEditorHarness extends ComponentHarness {
    constructor() {
        super(...arguments);
        this.getMonacoEditorElement = this.locatorFor('.monaco-editor');
        this.getMonacoInputAreaElement = this.locatorFor('textarea');
    }
    static { this.hostSelector = 'ngx-monaco-editor'; }
    static with(options) {
        return new HarnessPredicate(NgxMonacoEditorHarness, options)
            .addOption('testid', options.testid, (harness, text) => HarnessPredicate.stringMatches(harness.getTestid(), text));
    }
    async getTestid() {
        return (await this.host()).getAttribute("data-testid");
    }
    async click() {
        return (await this.getMonacoInputAreaElement()).click();
    }
    async focus() {
        return (await this.getMonacoInputAreaElement()).focus();
    }
    async blur() {
        return (await this.getMonacoInputAreaElement()).blur();
    }
    async isFocused() {
        return (await this.getMonacoEditorElement()).hasClass("focused");
    }
    async getText() {
        return (await this.getMonacoInputAreaElement()).getProperty('value');
    }
    async setValue(value) {
        const elem = await this.getMonacoInputAreaElement();
        await elem.focus();
        await elem.setInputValue(value);
        await elem.sendKeys(' ', TestKey.BACKSPACE); // to notify changes
    }
}

const noop = () => {
    // empty method
};
class NgxMonacoEditorFakeComponent {
    constructor() {
        this.value = model('');
        this.options = input({});
        this.language = input('typescript');
        this.editorStyle = input({
            width: "100%",
            height: "100%",
            border: "1px solid grey"
        });
        this.theme = input('vs');
        this.fullScreenKeyBinding = input();
        /**
         * Event emitted when editor is first initialized
         */
        this.editorInitialized = output();
        this.onFocus = output({ alias: "focus" });
        this.onBlur = output({ alias: "blur" });
        this.editorContainer = viewChild.required('editorContainer');
        this.focused = signal(false);
        this.zone = inject(NgZone);
        this.document = inject(DOCUMENT);
        this.cd = inject(ChangeDetectorRef);
        this.elementRef = inject((ElementRef));
        this.editor = viewChild('editor');
        this.propagateChange = noop;
        this.onTouched = noop;
    }
    ngOnInit() {
        this.editorInitialized.emit({
            // TODO: inject mock by provider config
            editor: {},
            monaco: {}
        });
    }
    focus() {
        this.editor()?.nativeElement?.focus();
    }
    ngOnChanges(changes) {
    }
    setValue(value) {
        this.propagateChange(value);
        this.value.set(value);
    }
    writeValue(value) {
        value = value ?? "";
        this.value.set(value);
    }
    registerOnChange(fn) {
        this.propagateChange = fn;
    }
    registerOnTouched(fn) {
        this.onTouched = fn;
    }
    ngOnDestroy() {
    }
    showFullScreenEditor() {
        const codeEditorElement = this.editorContainer().nativeElement;
        codeEditorElement.requestFullscreen();
    }
    exitFullScreenEditor() {
        this.document.exitFullscreen();
    }
    changedFocus(focused) {
        if (focused) {
            this.onFocus.emit();
        }
        else {
            this.onBlur.emit();
        }
        this.focused.set(true);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.3.4", ngImport: i0, type: NgxMonacoEditorFakeComponent, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.2.0", version: "17.3.4", type: NgxMonacoEditorFakeComponent, isStandalone: true, selector: "ngx-monaco-editor", inputs: { value: { classPropertyName: "value", publicName: "value", isSignal: true, isRequired: false, transformFunction: null }, options: { classPropertyName: "options", publicName: "options", isSignal: true, isRequired: false, transformFunction: null }, language: { classPropertyName: "language", publicName: "language", isSignal: true, isRequired: false, transformFunction: null }, editorStyle: { classPropertyName: "editorStyle", publicName: "editorStyle", isSignal: true, isRequired: false, transformFunction: null }, theme: { classPropertyName: "theme", publicName: "theme", isSignal: true, isRequired: false, transformFunction: null }, fullScreenKeyBinding: { classPropertyName: "fullScreenKeyBinding", publicName: "fullScreenKeyBinding", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { value: "valueChange", editorInitialized: "editorInitialized", onFocus: "focus", onBlur: "blur" }, host: { listeners: { "click": "focus()" }, properties: { "class.focused": "focused()" } }, providers: [
            {
                provide: NG_VALUE_ACCESSOR,
                useExisting: forwardRef(() => NgxMonacoEditorFakeComponent),
                multi: true,
            },
        ], viewQueries: [{ propertyName: "editorContainer", first: true, predicate: ["editorContainer"], descendants: true, isSignal: true }, { propertyName: "editor", first: true, predicate: ["editor"], descendants: true, isSignal: true }], usesOnChanges: true, ngImport: i0, template: `
    <div class="ngx-editor-container" #editorContainer [ngStyle]="editorStyle()">
      <textarea #editor [ngModel]="value()" (ngModelChange)="setValue($event)"
                (focus)="changedFocus(true)"
                (blur)="changedFocus(false)">
      </textarea>
    </div>`, isInline: true, styles: ["ngx-monaco-editor{display:block;position:relative}ngx-monaco-editor .ngx-editor-container{position:absolute;inset:0}ngx-monaco-editor .ngx-editor-container textarea{height:100%;width:100%}\n"], dependencies: [{ kind: "ngmodule", type: CommonModule }, { kind: "directive", type: i1.NgStyle, selector: "[ngStyle]", inputs: ["ngStyle"] }, { kind: "ngmodule", type: FormsModule }, { kind: "directive", type: i2.DefaultValueAccessor, selector: "input:not([type=checkbox])[formControlName],textarea[formControlName],input:not([type=checkbox])[formControl],textarea[formControl],input:not([type=checkbox])[ngModel],textarea[ngModel],[ngDefaultControl]" }, { kind: "directive", type: i2.NgControlStatus, selector: "[formControlName],[ngModel],[formControl]" }, { kind: "directive", type: i2.NgModel, selector: "[ngModel]:not([formControlName]):not([formControl])", inputs: ["name", "disabled", "ngModel", "ngModelOptions"], outputs: ["ngModelChange"], exportAs: ["ngModel"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.3.4", ngImport: i0, type: NgxMonacoEditorFakeComponent, decorators: [{
            type: Component,
            args: [{ selector: 'ngx-monaco-editor', standalone: true, imports: [
                        CommonModule,
                        NgClass,
                        NgStyle,
                        FormsModule,
                    ], template: `
    <div class="ngx-editor-container" #editorContainer [ngStyle]="editorStyle()">
      <textarea #editor [ngModel]="value()" (ngModelChange)="setValue($event)"
                (focus)="changedFocus(true)"
                (blur)="changedFocus(false)">
      </textarea>
    </div>`, host: {
                        "[class.focused]": "focused()",
                        "(click)": "focus()"
                    }, changeDetection: ChangeDetectionStrategy.OnPush, encapsulation: ViewEncapsulation.None, providers: [
                        {
                            provide: NG_VALUE_ACCESSOR,
                            useExisting: forwardRef(() => NgxMonacoEditorFakeComponent),
                            multi: true,
                        },
                    ], styles: ["ngx-monaco-editor{display:block;position:relative}ngx-monaco-editor .ngx-editor-container{position:absolute;inset:0}ngx-monaco-editor .ngx-editor-container textarea{height:100%;width:100%}\n"] }]
        }] });

class NgxMonacoEditorFakeHarness extends ComponentHarness {
    constructor() {
        super(...arguments);
        this.getMonacoInputAreaElement = this.locatorFor('textarea');
    }
    static { this.hostSelector = 'ngx-monaco-editor'; }
    static with(options) {
        return new HarnessPredicate(NgxMonacoEditorFakeHarness, options)
            .addOption('testid', options.testid, (harness, text) => HarnessPredicate.stringMatches(harness.getTestid(), text));
    }
    async getTestid() {
        return (await this.host()).getAttribute("data-testid");
    }
    async click() {
        return (await this.getMonacoInputAreaElement()).click();
    }
    async focus() {
        return (await this.getMonacoInputAreaElement()).focus();
    }
    async blur() {
        return (await this.getMonacoInputAreaElement()).blur();
    }
    async isFocused() {
        return (await this.getMonacoInputAreaElement()).isFocused();
    }
    async getText() {
        return (await this.getMonacoInputAreaElement()).getProperty('value');
    }
    async setValue(value) {
        const elem = await this.getMonacoInputAreaElement();
        await elem.focus();
        await elem.sendKeys(value);
    }
}

/**
 * Generated bundle index. Do not edit.
 */

export { NgxMonacoEditorFakeComponent, NgxMonacoEditorFakeHarness, NgxMonacoEditorHarness };
//# sourceMappingURL=jean-merelis-ngx-monaco-editor-testing.mjs.map
