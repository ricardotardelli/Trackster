import { InjectionToken } from "@angular/core";
import { editor, languages } from 'monaco-editor/esm/vs/editor/editor.api';
export interface MonacoAPI {
    editor: typeof editor;
    languages: typeof languages;
}
export interface MonacoLoader {
    monacoLoaded(): Promise<MonacoAPI>;
}
export declare const NGX_MONACO_LOADER_PROVIDER: InjectionToken<MonacoLoader>;
export declare class DefaultMonacoLoader implements MonacoLoader {
    private monacoPromise?;
    private config;
    constructor(config?: any, loadOnCreate?: boolean);
    monacoLoaded(): Promise<MonacoAPI>;
    private createPromise;
}
